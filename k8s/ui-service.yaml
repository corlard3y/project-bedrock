apiVersion: apps/v1
kind: Deployment
metadata:
  name: ui-service
  namespace: retail-store
  labels:
    app: ui-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ui-service
  template:
    metadata:
      labels:
        app: ui-service
    spec:
      containers:
      - name: ui-service
        image: public.ecr.aws/aws-containers/retail-store-sample-ui:1.0.0
        ports:
        - containerPort: 8080
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: ui-service
  namespace: retail-store
spec:
  selector:
    app: ui-service
  ports:
  - port: 80
    targetPort: 8080
  type: LoadBalancer
